TODO 列表（Miao3trike 功能状态与要点）
==============================

状态标记：☐ 未开始 / 🚧 进行中 / ✅ 完成 / ⚠️ 失败

1) 划火柴（拖动录制宏）
   - ✅ 录制：无障碍全屏 overlay 捕获单次拖动；绘制起止/箭头指示。
   - ✅ 坐标：按钮中心支持校准（可拖动指示点，持久化保存）；未设置时使用 rx/ry 推导。
   - ✅ 宏序列：按钮→拖动（终点不松手）→返回→终点悬停→松手；步骤间延迟使用配置项。
   - ✅ 录制结束优化：overlay 先隐藏并设为不可触摸，再尽快启动宏。
   - ✅ 兼容降级：不支持 continueStroke 的设备降级为普通拖动后返回。

2) 放技能（点击捕获宏）
   - ✅ 触发：音量+进入点击录制（overlay 捕获单点）。
   - ✅ 退出：录制中再次按音量+或点击悬浮球立即退出。
   - ✅ 宏序列：按钮→点击点→按钮；步骤间延迟使用配置项。
   - ✅ 录制结束优化：overlay 先隐藏并设为不可触摸，再尽快启动宏。

3) 延迟参数设置（主界面）
   - ✅ “启动延迟 / 步骤间延迟 / 拖动速度 / 悬停延迟（以便干员落位）”，单位 ms。
   - ✅ 支持保存与恢复初始设定（SharedPreferences）。

4) 悬浮球状态指示
   - ✅ 正常开关状态使用原图标。
   - ✅ 点击录制时显示“未激活但不同配色”（橙色）以区分状态。

5) 放技能开关（菜单）
   - ✅ 在设置菜单新增开关，用于启用/禁用“放技能”（点击捕获宏）。

6) 菜单样式调整（放技能开关位置与配色）
   - ✅ 将“放技能”开关移到菜单最下方，并将菜单文本改为白色、输入框文字为黑色。

7) 步进（音量-）
   - ✅ 音量-触发“点击按钮→等待步进延迟→再次点击按钮”；菜单增加“步进延迟”输入框与功能开关。

8) ✅ 品牌名称替换为 Miao3trikeMod
   - ✅ 替换应用内显示文案、程序名与无障碍服务名（含通知/提示文案）。

9) ✅ 包名调整为 com.miao3strikemod.matches
   - ✅ 修正源码 package 声明与目录结构不一致导致的报错。

10) ✅ 清理 Gradle 文件 BOM
   - ✅ 移除 build.gradle 头部 BOM 以修复编译报错。

11) ✅ 清理 Java 文件 BOM
   - ✅ 移除源码 .java 头部 BOM 以修复编译报错。

12) ✅ 调整延迟默认值
   - ✅ 步骤间延迟/拖动速度/悬停延迟/步进延迟默认与初始值更新。

13) ✅ 取消拖动速度最小值限制
   - ✅ 允许拖动速度小于 50ms（保留初始值为 50ms）。

14) ✅ 总开关联动音量宏
   - ✅ 关闭 @+id/btn_toggle_service 时，禁用音量+/- 相关功能并终止运行中的宏。

15) ✅ 修复设置菜单亮色模式文字不可见
   - ✅ 亮色模式背景为白色时保持菜单可读性（文字/输入框对比度）。

16) ✅ 修改版本号到 1.1
   - ✅ versionName 更新为 1.1（app/build.gradle）。

17) ✅ 修复“划火柴”模式蓝色指示点无法拖到屏幕最边缘
   - ✅ 使用真实屏幕尺寸并允许 overlay 覆盖系统导航栏/手势区域（与 1.2 版本合并提交）。

18) ✅ 版本号更新到 1.2
   - ✅ 更新 versionName 为 1.2。
